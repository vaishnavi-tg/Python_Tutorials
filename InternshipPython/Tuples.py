# Tuples are immutable
m=(1,2,3,4,5,6,7,8,9)
print(type(m))
list(m)
print(type(m))
# Tuples are static sequence , u can typeconvert,immuteble
# u cant add or sort or watever
# List and Tuples differences
# Elements in tuples shud be printed in x format 
# X pattern using the elements in tuples
t=(1,2,3,4,5,6,7,8,9)
